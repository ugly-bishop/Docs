---
title: "List Payouts"
sidebarTitle: "List Payouts"
api: "GET /v1/payouts"
description: "Retrieve historical payouts executed by this merchant account."
---

Fetches historical payout transactions initiated by your merchant account.

### Scopes required

`payouts:read` or `*`

### Headers

<ParamField header="Authorization" type="string" required>
  Bearer API secret key formatted as `Bearer sk_live_...` or `Bearer sk_test_...`.
</ParamField>

### Response

<ResponseField name="payouts" type="array">
  Array of payout transaction records.

  <Expandable title="Transaction properties">
    <ResponseField name="id" type="string">
      Transaction UUID.
    </ResponseField>
    <ResponseField name="reference" type="string">
      Payout reference code.
    </ResponseField>
    <ResponseField name="type" type="string">
      Transaction type (`PAYOUT`).
    </ResponseField>
    <ResponseField name="status" type="string">
      Settlement status: `PROCESSING`, `PENDING`, `SUCCESS`, or `FAILED`.
    </ResponseField>
    <ResponseField name="currency" type="string">
      Currency code.
    </ResponseField>
    <ResponseField name="amountMinor" type="string">
      Amount in minor currency units as string.
    </ResponseField>
    <ResponseField name="ngnAmountMinor" type="string">
      Total NGN debit in minor units.
    </ResponseField>
    <ResponseField name="recipient" type="string">
      Beneficiary name.
    </ResponseField>
    <ResponseField name="createdAt" type="string">
      Creation timestamp.
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://api.tabsglobal.co/v1/payouts" \
  -H "Authorization: Bearer sk_live_..."
```

```javascript Node.js
const response = await fetch("https://api.tabsglobal.co/v1/payouts", {
  method: "GET",
  headers: {
    "Authorization": `Bearer ${process.env.TABS_SECRET_KEY}`,
  },
});

const payouts = await response.json();
console.log(payouts);
```

```python Python
import requests

response = requests.get(
    "https://api.tabsglobal.co/v1/payouts",
    headers={"Authorization": f"Bearer {API_KEY}"},
)
print(response.json())
```
</RequestExample>

<ResponseExample>
```json 200 OK
[
  {
    "id": "7d9c0245-31ba-4f27-a0c3-fba4313f89e2",
    "reference": "PO-AB12CD34",
    "type": "PAYOUT",
    "status": "SUCCESS",
    "currency": "NGN",
    "amountMinor": "2500000",
    "ngnAmountMinor": "2500000",
    "recipient": "ADEWALE ADELEKE",
    "metadata": {
      "railReference": "NIP-9948102941",
      "provider": "embedly"
    },
    "completedAt": "2026-09-21T02:00:15.000Z",
    "createdAt": "2026-09-21T02:00:00.000Z"
  }
]
```
</ResponseExample>
